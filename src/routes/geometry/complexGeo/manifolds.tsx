function Manifolds(){
    return(
        <p>I'm going to assume the reader already knows what a manifold is in the smooth context, if not the image below gives a quick summary.</p>

        <p>Giiven a manifold M </p>
    );
};


export default Manifolds;